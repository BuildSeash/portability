from distutils.core import setup
setup(
  name = 'portability',
  packages = [''], 
  version = '0.1',
  description = "Seattle's portability package",
  author = 'Seattle',
  url = 'https://github.com/choksi81/portability', # use the URL to the github repo
  classifiers = [],
)